import { CcvDirective } from './ccv.directive';

describe('CcvDirective', () => {
  it('should create an instance', () => {
    const directive = new CcvDirective();
    expect(directive).toBeTruthy();
  });
});
